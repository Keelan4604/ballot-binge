# Database URL — SQLite for local dev, Postgres for production
# SQLite (local):
DATABASE_URL="file:./dev.db"

# PostgreSQL (production):
# DATABASE_URL="postgresql://user:password@host:5432/ballot_binge?schema=public"
